export const name = (state) => state.orderDetails.name;
export const number = (state) => state.orderDetails.number;
export const status = (state) => state.orderDetails.status;
export const error = (state) => state.orderDetails.error;
export const visible = (state) => state.orderDetails.visible;
