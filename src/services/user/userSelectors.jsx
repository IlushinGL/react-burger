export const name = (state) => state.currentUser.name;
export const email = (state) => state.currentUser.email;
export const status = (state) => state.currentUser.status;
export const error = (state) => state.currentUser.error;
export const auth = (state) => state.currentUser.auth;
